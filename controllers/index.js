const blogRoutes = require('./api/BLOG_ROUTES');
const commentRoutes = require('./api/COMMENT_ROUTES');
const dashboardRoutes = require('./api/DASHBOARD_ROUTES');
const userRoutes = require('./api/USER_ROUTES');

module.exports = {
  blogRoutes,
  commentRoutes,
  dashboardRoutes,
  userRoutes,
};
